version: "2"
run:
  timeout: 5m
  allow-parallel-runners: true
linters:
  default: none
  enable:
    - dupl
    - errcheck
    - copyloopvar
    - ginkgolinter
    - goconst
    - gocyclo
    - govet
    - ineffassign
    - lll
    - misspell
    - nakedret
    - prealloc
    - revive
    - staticcheck
    - unconvert
    - unused
  settings:
    revive:
      rules:
        - name: comment-spacings
  exclusions:
    generated: lax
    rules:
      - path: _test.go
        linters:
          - goconst
          - dupl
      - path: "api/.*"
        linters:
          - lll
      - path: "internal/.*"
        linters:
          - dupl
          - lll
formatters:
  enable:
    - gofmt
    - goimports
