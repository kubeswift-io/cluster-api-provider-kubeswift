---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: kubeswiftclustertemplates.infrastructure.cluster.x-k8s.io
spec:
  group: infrastructure.cluster.x-k8s.io
  names:
    categories:
    - cluster-api
    kind: KubeSwiftClusterTemplate
    listKind: KubeSwiftClusterTemplateList
    plural: kubeswiftclustertemplates
    singular: kubeswiftclustertemplate
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: KubeSwiftClusterTemplate is the Schema for the kubeswiftclustertemplates
          API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: KubeSwiftClusterTemplateSpec defines the desired state of
              KubeSwiftClusterTemplate.
            properties:
              template:
                description: template is the KubeSwiftCluster that ClusterClass stamps
                  out.
                properties:
                  metadata:
                    description: metadata is the labels and annotations to stamp onto
                      the created object.
                    minProperties: 1
                    properties:
                      annotations:
                        additionalProperties:
                          type: string
                        description: |-
                          annotations is an unstructured key value map stored with a resource that may be
                          set by external tools to store and retrieve arbitrary metadata. They are not
                          queryable and should be preserved when modifying objects.
                          More info: http://kubernetes.io/docs/user-guide/annotations
                        type: object
                      labels:
                        additionalProperties:
                          type: string
                        description: |-
                          labels is a map of string keys and values that can be used to organize and categorize
                          (scope and select) objects. May match selectors of replication controllers
                          and services.
                          More info: http://kubernetes.io/docs/user-guide/labels
                        type: object
                    type: object
                  spec:
                    description: spec is the KubeSwiftClusterSpec to stamp out.
                    properties:
                      controlPlaneEndpoint:
                        description: |-
                          controlPlaneEndpoint is the endpoint used to reach the workload cluster's
                          Kubernetes API server. It is the resolved endpoint: with endpoint.mode=External
                          the operator supplies it (a control-plane VIP or external load balancer); with
                          endpoint.mode=Service or RoutableIP the provider provisions and writes it. Once
                          it is set, the controller reports the cluster provisioned and the core Cluster
                          controller surfaces it onto Cluster.spec.controlPlaneEndpoint.
                        minProperties: 1
                        properties:
                          host:
                            description: host is the hostname on which the API server
                              is serving.
                            maxLength: 512
                            minLength: 1
                            type: string
                          port:
                            description: port is the port on which the API server
                              is serving.
                            format: int32
                            maximum: 65535
                            minimum: 1
                            type: integer
                        type: object
                      endpoint:
                        description: |-
                          endpoint configures how the provider establishes the control-plane endpoint.
                          When unset (or mode=External), the operator supplies controlPlaneEndpoint and the
                          provider provisions nothing.
                        properties:
                          mode:
                            default: External
                            description: |-
                              mode selects the endpoint strategy:
                                External — the operator supplies spec.controlPlaneEndpoint; the provider
                                           provisions nothing.
                                Service  — the provider mints a Kubernetes Service in the management cluster
                                           fronting the control-plane guests on the API-server port, and
                                           reports its address as the endpoint. CNI-agnostic (no OVN needed).
                              (RoutableIP — use the control-plane guest's OVN-routable IP — is planned; see
                              docs/design/control-plane-endpoint.md.)
                            enum:
                            - External
                            - Service
                            type: string
                          service:
                            description: service configures the Service-backed endpoint.
                              Used when mode=Service.
                            properties:
                              annotations:
                                additionalProperties:
                                  type: string
                                description: |-
                                  annotations are copied onto the Service (e.g. a MetalLB address pool or an
                                  external-dns hostname). Chiefly useful with type=LoadBalancer.
                                type: object
                              loadBalancerClass:
                                description: |-
                                  loadBalancerClass selects a specific load-balancer implementation. Only valid with
                                  type=LoadBalancer.
                                type: string
                              port:
                                default: 6443
                                description: port is the API-server port fronted by
                                  the Service.
                                format: int32
                                maximum: 65535
                                minimum: 1
                                type: integer
                              type:
                                default: ClusterIP
                                description: |-
                                  type is the Service type fronting the control-plane guests. ClusterIP is reachable
                                  from the management cluster (the CAPI controllers and worker VMs) and is enough for
                                  a CAPI-managed cluster to form; LoadBalancer additionally exposes the endpoint
                                  outside the management cluster (e.g. for operator kubectl access).
                                enum:
                                - ClusterIP
                                - LoadBalancer
                                type: string
                            type: object
                        type: object
                      guestNamespace:
                        description: |-
                          guestNamespace is the namespace in which the SwiftGuest VMs backing this
                          cluster's machines are created. Defaults to the KubeSwiftCluster's namespace.
                        type: string
                    type: object
                required:
                - spec
                type: object
            required:
            - template
            type: object
        type: object
    served: true
    storage: true
    subresources: {}
